---
/**
 * Home page — the homelab as a live system diagram (see Topology.astro).
 *
 * This is the "Topology" UI direction: the landing view IS the system. The old
 * editorial hero + status strip is replaced; /cv and /about keep their current
 * styling until the full overhaul lands.
 */
import Base from '../layouts/Base.astro';
import Topology from '../components/Topology.astro';
---

<Base>
  <Topology />
</Base>
